apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.grafanaProvisioning.dashboards.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  test.json: |
    {{ .Files.Get "dashboards/test.json" | fromJson | toJson | indent 4 }}
